#!/usr/bin/node
const myLangs = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let index = 0; index < myLangs.length; index++) {
  const element = myLangs[index];
  console.log(element);
}
