---------- Importing the ``My_list`` function ----------
	>>> my_list = __import__('1-my_list').my_list

    ==================== Principal requirements checkers ====================


	---------- Check that the file is executable ----------
	>>> import os
	>>> os.access('1-my_list.py', os.X_OK)
	True

	---------- Check for the documentation of the module ----------
	>>> len(__import__("1-my_list").__doc__) > 10
	True

	---------- Check for the documentation of the function ----------
	>>> len(__import__("1-my_list").add_integer.__doc__) > 10
	True

	---------- Check for pep8 style ----------
	>>> os.popen("pep8 1-my_list.py").read()
	''

	---------- Check for a newline at the end of the file ----------
	>>> os.popen("cat -e 1-my_list.py | tail -1").read()[-1]
	'\n'

	---------- Check for the file has a shebang ----------
	>>> os.popen("cat 1-my_list.py | head -1").read()
	'#!/usr/bin/python3\n'

	---------- Check that the README file exists ----------
	>>> cwd = os.getcwd()
	>>> check_readme = cwd + '/README.md'
	>>> os.path.exists(check_readme)
	True

    ==================== Testing cases ====================

    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> my_list.append(3)
    >>> my_list.append(5)
    >>> print(my_list)
    [1, 4, 2, 3, 5]
    >>> my_list.print_sorted()
    [1, 2, 3, 4, 5]
    >>> print(my_list)
    [1, 4, 2, 3, 5]

    >>> my_list = MyList()
    >>> my_list.append(-1)
    >>> my_list.append(-8)
    >>> my_list.append(-2)
    >>> my_list.append(-7)
    >>> my_list.append(-3)
    >>> print(my_list)
    [-1, -8, -2, -7, -3]
    >>> my_list.print_sorted()
    [-8, -7, -3, -2, -1]
    >>> print(my_list)
    [-1, -8, -2, -7, -3]

    >>> my_list = MyList()
    >>> my_list.print_sorted()
    []
